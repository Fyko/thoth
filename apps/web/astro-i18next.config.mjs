/** @type {import('astro-i18next').AstroI18nextConfig} */
export default {
  defaultLocale: "en-US",
  locales: ["en-US"],
  i18nextServer: {
    supportedLngs: ["en-US"],
    // debug: true,
  },
};
